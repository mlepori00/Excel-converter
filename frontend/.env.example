# Backend API URL
# Default (local dev): http://localhost:8000
# Change this if the backend runs on a different host
VITE_API_URL=http://localhost:8000

# Shared secret token – must match API_SECRET_TOKEN in the backend .env
VITE_API_TOKEN=DEIN_TOKEN_HIER
